/**
 * Barrel de tipos globales
 *
 * Este archivo reexporta todos los tipos principales del dominio de la aplicación
 * para facilitar imports cortos y centralizados.
 *
 * @example
 * ```ts
 * // Importar tipos de usuario y loader desde el barrel
 * import type { User, LoaderVariant } from '@types';
 * ```
 */
export * from './auth.types';
export * from './loader.types';

