/**
 * Barrel del componente PWA Update
 *
 * Exporta el componente PWAUpdateComponent para facilitar imports centralizados.
 *
 * @example
 * ```tsx
 * // Importar el componente PWA Update
 * import { PWAUpdateComponent } from '@components';
 *
 * function App() {
 *   return (
 *     <div>
 *       <PWAUpdateComponent />
 *       <Router>
 *         <Routes>
 *           <Route path="/" element={<HomePage />} />
 *         </Routes>
 *       </Router>
 *     </div>
 *   );
 * }
 * ```
 */
export * from './PWAUpdateComponent';
export { PWAUpdateDebug } from './PWAUpdateDebug';

